{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main + panel windows",
  "windows": ["main", "panel"],
  "permissions": [
    "core:default",
    "core:window:allow-set-theme",
    "opener:default",
    "updater:default",
    "process:default"
  ]
}
